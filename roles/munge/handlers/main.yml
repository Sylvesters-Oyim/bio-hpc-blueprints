---
# Handlers for the munge role.
# NOTE: added during repo assembly — the munge task file notifies "restart munge"
# but no handler file was present. This is the standard implementation.
#
# Author: Sylvesters Ochieng Oyim, Rhodes University

- name: restart munge
  ansible.builtin.systemd:
    name: munge
    state: restarted
