# Structure similarity (US-align)
TM-score of every pair of structures in `input/`.

    ./submit.sh

Results: `results/pairs.tsv`, `results/tm_matrix.tsv`, `results/tm_heatmap.png`.
TM-score > 0.5 = same fold, < 0.3 = unrelated.
