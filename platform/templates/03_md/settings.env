# Structure in input/
PDB=protein.pdb
# Production length (ns)
LENGTH_NS=1
# Temperature (K)
TEMPERATURE=300
# Force field
FORCEFIELD=amber99sb-ildn
# Node to run on (empty = the one with most CPUs)
NODE=
