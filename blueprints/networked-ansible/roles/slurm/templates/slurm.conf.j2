# Generated by Ansible. Edit this template and re-run, not the file on the nodes.
ClusterName={{ cluster_name }}
SlurmctldHost={{ control_machine }}({{ hostvars[control_machine].node_addr }})

SlurmUser={{ slurm_user }}
StateSaveLocation=/var/spool/slurmctld
SlurmdSpoolDir=/var/spool/slurmd
SlurmctldPidFile=/run/slurmctld.pid
SlurmdPidFile=/run/slurmd.pid

AuthType=auth/munge
SlurmctldPort=6817
SlurmdPort=6818

# Simple first boot. NOT DONE YET: cgroup limits (see blueprints/offline-usb for a working cgroup setup).
ProctrackType=proctrack/linuxproc
TaskPlugin=task/none
SchedulerType=sched/backfill
SelectType=select/cons_tres
SelectTypeParameters=CR_CPU_Memory
ReturnToService=2

SlurmctldTimeout=120
SlurmdTimeout=300

SlurmctldLogFile=/var/log/slurm/slurmctld.log
SlurmdLogFile=/var/log/slurm/slurmd.log

# GPUs: uncomment, add slurm_gres=gpu:N to the node in inventory, add gres.conf on that node.
# GresTypes=gpu

# One line per inventory host
{% for host in groups['cluster'] %}
NodeName={{ host }} NodeAddr={{ hostvars[host].node_addr }} CPUs={{ hostvars[host].slurm_cpus }} RealMemory={{ hostvars[host].slurm_realmem }}{% if hostvars[host].slurm_gres is defined %} Gres={{ hostvars[host].slurm_gres }}{% endif %} State=UNKNOWN
{% endfor %}

PartitionName=main Nodes=ALL Default=YES MaxTime=INFINITE State=UP
